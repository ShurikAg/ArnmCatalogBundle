{% extends 'ArnmAraneumBundle::araneum.html.twig' %}

{% form_theme form 'ArnmAraneumBundle:Form:araneum.html.twig' %}

{% block sidebar %}
	<div class="alert alert-info hint">
		<span class="label label-info">{{ 'default.label.info'|trans }}</span><br/>
  		{{ 'catalog.category.edit.info'|trans({}, 'catalog')|raw }}
	</div>
{% endblock %}

{% block title %}
	{{ parent() }} | {{ 'catalog.category.form.title.edit'|trans({}, 'catalog') }}
{% endblock %}

{% block content %}
	<div class="page-header">
		<h2>{{ 'catalog.category.form.title.edit'|trans({}, 'catalog') }}</h2>
	</div>
	
	{% include 'ArnmAraneumBundle:Misc:messages.html.twig' %}
	
	{% if form %}
		<form action="{{ path('arnm_catalog_category_edit', { 'id': category.id }) }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
			<fieldset>
				{{ form_widget(form) }}
				<div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
    			    	<input class="btn btn-primary" type="submit" value="{{ 'catalog.category.form.action.update'|trans({}, 'catalog') }}"/>
    			    	<input class="btn" type="reset" value="{{ 'default.form.action.reset'|trans }}"/>
    			    	<a  class="btn" href="{{ path('arnm_catalog_categories') }}">{{ 'default.form.action.back_to_list'|trans }}</a>
    			    </div>
			    </div>
		    </fieldset>
		</form>
	{% endif%}
{% endblock %}
